/*
 * Archivo que genera el contexto, creando un objeto contexto mediante la funcion create context
 */

import { createContext } from "react";
const UserContext = createContext();

export default UserContext;
